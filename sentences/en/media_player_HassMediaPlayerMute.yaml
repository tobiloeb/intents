language: en
intents:
  HassMediaPlayerMute:
    data:
      - sentences:
          - "mute"
          - "mute ([the] (music|media [player[s]])) [<here>]"
        requires_context:
          area:
            slot: true
      - sentences:
          - "mute <name>"
          - "mute ([the] (music|media [player[s]])) on <name>"
        requires_context:
          domain: media_player
